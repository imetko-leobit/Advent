# ====================================================================================
# Well Being Quest - Environment Configuration
# ====================================================================================
# Copy this file to .env.local and fill in the actual values
# DO NOT commit .env.local to version control (it's in .gitignore)
# ====================================================================================

# ====================================================================================
# OIDC Authentication Configuration (REQUIRED)
# ====================================================================================

# VITE_APP_AUTH_AUTHORITY (REQUIRED)
# The base URL of your OIDC/OAuth2 identity provider
# This is the URL that will be used to discover OIDC endpoints via /.well-known/openid-configuration
# 
# Examples:
#   Keycloak: https://keycloak.example.com/realms/leobit
#   Auth0: https://leobit.auth0.com
#   Azure AD: https://login.microsoftonline.com/{tenant-id}/v2.0
#   Okta: https://your-domain.okta.com
#
# Without this value, authentication will fail with:
#   "Oops... No authority or metadataUrl configured on settings"
VITE_APP_AUTH_AUTHORITY=

# VITE_APP_AUTH_REDIRECT_URI (REQUIRED)
# The complete URL where users will be redirected after authentication
# This MUST exactly match a redirect URI configured in your OIDC provider for client: leobit.quest.web
#
# Format: {protocol}://{host}:{port}/login
# 
# Examples:
#   Local Development: http://localhost:3000/login
#   Production: https://quest.leobit.com/login
#   Test Environment: https://quest-test.leobit.com/login
#
# Notes:
#   - The port should match the one in vite.config.ts (default: 3000)
#   - Include the /login path (the login page route)
#   - This same URL is used for post_logout_redirect_uri
#   - Must be registered in OIDC provider's allowed redirect URIs
VITE_APP_AUTH_REDIRECT_URI=

# ====================================================================================
# Application Data Source (REQUIRED)
# ====================================================================================

# VITE_GOOGLE_SHEET_URL (REQUIRED)
# Google Sheets CSV export URL containing quest participant data
#
# Format: https://docs.google.com/spreadsheets/d/{SHEET_ID}/export?format=csv
# 
# The Google Sheet must contain the following columns:
#   - Email Address: Employee email (used for user identification)
#   - Ім'я та прізвище: Full name in Ukrainian
#   - Соц мережі відмітки: Social network points (numeric)
#   - 1. Список справ, 2. Пункт зі списку, etc.: Task completion timestamps
#
# Notes:
#   - The sheet must be publicly accessible or accessible to authenticated users
#   - Data is polled every 3 minutes while the application is running
#   - Changes to sheet structure may require code updates in src/helpers/userDataMapper.ts
VITE_GOOGLE_SHEET_URL=

# ====================================================================================
# Additional Notes
# ====================================================================================
# 
# OIDC Client Configuration (hardcoded in src/auth/config.ts):
#   - client_id: leobit.quest.web
#   - scope: quest openid profile
#   - The "quest" scope must be configured in your OIDC provider
#
# OIDC Provider Requirements:
#   - Must support OIDC Discovery (/.well-known/openid-configuration)
#   - Must support Authorization Code Flow with PKCE
#   - Must provide "sub" and "email" claims in user profile
#
# For detailed setup instructions, see: OIDC_CONFIGURATION.md
# ====================================================================================