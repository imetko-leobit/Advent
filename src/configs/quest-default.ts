/**
 * Default Well Being Quest Configuration
 * 
 * This is the original/default quest configuration
 * Re-exported from the main config for consistency
 */

import { questConfig } from "../config/quest.config";

/**
 * Default Quest Configuration
 * This is the standard Well Being Quest
 */
export default questConfig;
